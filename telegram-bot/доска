Ты — техред/архитектор проекта “Калькулятор теплиц + короткое КП + база знаний”.
Мне нужно подготовить пакет файлов и краткий отчёт, чтобы другой помощник (ChatGPT) быстро вошёл в контекст и не путался.

Задача: 
1) перечислить текущие файлы проекта и роль каждого;
2) подготовить экспорт/копипасту ключевых файлов и конфигов;
3) кратко описать, что уже работает и что не доделано;
4) отдельно: где лежит логика “короткое КП”, где “длинное КП”, где выбор альтернативного варианта, где база знаний.

Ограничения:
- Никаких выдумок. Если чего-то нет — так и пиши.
- Никаких общих слов. Только конкретика: “в файле X функция Y делает Z”.
- Итог должен быть пригоден для того, чтобы я просто загрузил/переслал файлы и получил ревью.

Сформируй вывод в 3 блоках:

БЛОК 1 — КАРТА ПРОЕКТА (1 экран)
- Список файлов: имя → назначение → ключевые функции/компоненты
- Точки входа (какой файл запускается первым)
- Где хранятся данные (если есть): json, xlsx, supabase, локально и т.д.

БЛОК 2 — ЧТО СЕЙЧАС ГОТОВО (checklist)
✅ работает:
- ...
⚠️ частично:
- ...
❌ не сделано:
- ...

БЛОК 3 — ЧТО МНЕ НАДО ОТДАТЬ ДЛЯ РЕВЬЮ (файлы + куски)
Подготовь список “обязательных” и “желательных” файлов.

Обязательные:
- index.html (или главный html)
- scripts.js / index.js (главная логика)
- styles.css
- любые файлы с генерацией КП (короткое/длинное)
- любые файлы с загрузкой/парсингом данных (parser.js)
- supabase.js и railway.json (если используется)
- ИНСТРУКЦИЯ.md (если есть)
- пример входных данных: 1-2 типичных сценария расчёта (в виде текста: что вводим → что получаем)

Желательные:
- knowledge_base_v1.json (если уже есть)
- delivery_hubs.json (если есть)
- любые таблицы/Excel, которые реально используются в коде

В конце:
- напиши “что мне сейчас нужно загрузить в чат”, строго списком файлов.
- если файлов много, укажи “минимальный набор” из 4–6 файлов, чтобы ревью было возможным.

Начни прямо сейчас: выведи карту проекта и минимальный набор файлов.
