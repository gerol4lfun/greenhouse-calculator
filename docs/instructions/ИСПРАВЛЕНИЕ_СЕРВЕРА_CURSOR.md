# 🔧 Исправление проблемы с сервером Cursor

## Проблема
Cursor пытается сохранить файл с именем `image`, но это директория с изображениями.

## Решение (выполните по порядку):

### 1. Закройте все проблемные файлы
- Проверьте все открытые вкладки в Cursor
- Если есть файл с именем `image` (без расширения) - закройте его
- Закройте диалог ошибки (крестик или "Revert")

### 2. Перезапустите Cursor
- Нажмите `Cmd+Q` (полностью закрыть Cursor)
- Или через меню: `Cursor > Quit Cursor`
- Откройте Cursor заново

### 3. Переоткройте проект
- `File > Close Folder` (закрыть текущую папку)
- `File > Open Folder...` (открыть папку проекта заново)
- Выберите папку: `/Users/pavelkulcinskij/Downloads/delivery-calculator-test-main`

### 4. Если проблема останется
- Закройте все вкладки: `Cmd+K, Cmd+W`
- Перезапустите сервер разработки: `Cmd+Shift+P` → `Developer: Reload Window`

## ✅ Что я сделал:
- Создал файл `.cursorignore` - чтобы Cursor игнорировал проблемные файлы
- Создал настройки `.vscode/settings.json` - для правильной работы с файлами

## 📝 Примечание
Проект работает нормально! Проблема только в Cursor. HTML можно открывать напрямую в браузере.
