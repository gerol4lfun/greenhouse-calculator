# 📁 СТРУКТУРА ПРОЕКТА

## ✅ ОСНОВНАЯ ПАПКА (РАБОЧАЯ):
**`delivery-calculator-test-main/`** - это единственная рабочая папка!

## 📂 СТРУКТУРА ПАПКИ:

```
delivery-calculator-test-main/
├── index.html              ← ОТКРЫВАТЬ ЭТОТ ФАЙЛ!
├── css/
│   └── styles.css          ← Стили
├── js/
│   └── scripts.js          ← Основной скрипт (v59)
├── images/                 ← Изображения (если есть)
│   └── faq/               ← Изображения для FAQ
├── backup/                ← Старые версии (не трогать)
├── db/                    ← Миграции базы данных
├── sql/                   ← SQL скрипты
├── scripts/               ← Node.js скрипты для импорта
├── telegram-bot/          ← Telegram бот для дат доставки
├── archive/              ← Архивные данные (processed и т.д.)
│   └── processed/        ← Обработанные данные из старого проекта
└── *.md                   ← Документация
```

## ❌ СТАРЫЕ ПАПКИ (НЕ ИСПОЛЬЗОВАТЬ):
- `delivery-calculator-test-main 2_OLD_BACKUP/` - старая версия (v6), можно удалить
- `delivery-calculator-test-main — копия/` - старая копия, можно удалить

## 🚀 КАК РАБОТАТЬ:

1. **Открывать:** `delivery-calculator-test-main/index.html`
2. **Редактировать:** файлы в `delivery-calculator-test-main/`
3. **Не трогать:** папку `backup/` и старые папки

## 📝 ВЕРСИЯ:
Текущая версия: **v59**

Версия указана в:
- `js/scripts.js` - строка 4: `const APP_VERSION = "v59"`
- `index.html` - строка 357: `js/scripts.js?v=59`

## 🔧 ВОЗМОЖНЫЕ ПРОБЛЕМЫ:

### Ошибки в скриптах:
- Это может быть из-за ограничений sandbox при работе с файлами
- Или из-за синтаксических ошибок в коде
- Всегда проверяйте консоль браузера (F12) для диагностики

### Проблемы с файлами:
- Некоторые операции с файлами могут не работать из-за ограничений безопасности
- Используйте прямые пути к файлам, а не относительные
- Проверяйте права доступа к файлам
