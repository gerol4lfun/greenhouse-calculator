# 🚀 Быстрая инструкция: Создание базы знаний

## Проблема
Ошибка: `relation "public.knowledge_base" does not exist`

Это значит, что таблица в Supabase еще не создана.

## Решение (2 шага):

### ШАГ 1: Создать таблицу

1. Откройте **Supabase Dashboard**: https://supabase.com/dashboard
2. Выберите ваш проект
3. Перейдите в **SQL Editor** (слева в меню)
4. Откройте файл на компьютере: `db/migrations/20260203_create_knowledge_base.sql`
5. Скопируйте **весь** SQL код
6. Вставьте в SQL Editor
7. Нажмите **Run** (или F5)

**✅ Готово! Таблица создана.**

### ШАГ 2: Импортировать данные

1. Откройте терминал
2. Перейдите в папку проекта:
   ```bash
   cd /Users/pavelkulcinskij/Downloads/delivery-calculator-test-main
   ```
3. Установите зависимости (если еще не установлены):
   ```bash
   npm install
   ```
4. Создайте файл `.env` в корне проекта (если его нет):
   ```
   SUPABASE_URL=https://dyoibmfdohpvjltfaygr.supabase.co
   SUPABASE_SERVICE_ROLE_KEY=ваш_service_role_key
   ```
5. Импортируйте данные:
   ```bash
   npm run kb:import
   ```

**✅ Готово! Данные импортированы.**

## Проверка

После выполнения обоих шагов:
1. Обновите страницу калькулятора (F5)
2. Откройте "База знаний"
3. Должны увидеть карточки вместо ошибки

---

**Если что-то не работает** — проверьте:
- Таблица создана? (Supabase Dashboard → Table Editor → должна быть таблица `knowledge_base`)
- Данные импортированы? (в таблице должно быть 25 записей)
- Правильный `SUPABASE_SERVICE_ROLE_KEY` в `.env`?
