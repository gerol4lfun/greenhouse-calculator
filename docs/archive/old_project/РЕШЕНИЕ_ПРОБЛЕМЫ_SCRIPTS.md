# 🔧 Решение проблемы с scripts.js на GitHub

## 🎯 Проблема:
Файл `scripts.js` отображается как сплошной текст без подсветки синтаксиса и не дает сохранить на GitHub.

## ✅ Решение:

### Вариант 1: Загрузка через "Upload files" (РЕКОМЕНДУЕТСЯ)

1. Откройте: `https://github.com/gerol4lfun/teplitsa-calculator-main`
2. Нажмите **"Add file"** → **"Upload files"**
3. Перетащите файл `scripts.js` из папки `js/` на вашем компьютере
4. **ВАЖНО:** GitHub автоматически создаст папку `js/`, если её нет
5. Нажмите **"Commit changes"** (внизу справа)

**Если файл не загружается:**
- Проверьте, что файл называется именно `scripts.js` (с расширением `.js`)
- Убедитесь, что файл не поврежден
- Попробуйте загрузить файл меньшего размера для проверки

---

### Вариант 2: Создание через "Create new file"

1. Откройте: `https://github.com/gerol4lfun/teplitsa-calculator-main`
2. Нажмите **"Add file"** → **"Create new file"**
3. В поле имени файла введите: `js/scripts.js`
   - **ВАЖНО:** `js/` в начале создаст папку автоматически!
4. Откройте локальный файл `js/scripts.js` на вашем компьютере
5. Скопируйте **ВЕСЬ** текст (Ctrl+A, Ctrl+C)
6. Вставьте в поле на GitHub (Ctrl+V)
7. Прокрутите вниз и нажмите **"Commit new file"**

**Если не дает сохранить:**
- Проверьте, что в имени файла есть расширение `.js`
- Убедитесь, что скопировали весь текст
- Попробуйте сохранить по частям (но это не рекомендуется)

---

### Вариант 3: Использование GitHub Desktop (если установлен)

1. Откройте GitHub Desktop
2. Выберите репозиторий `teplitsa-calculator-main`
3. Скопируйте файл `js/scripts.js` в папку репозитория на вашем компьютере
4. GitHub Desktop автоматически обнаружит изменения
5. Напишите commit message: "Add scripts.js"
6. Нажмите **"Commit to main"**
7. Нажмите **"Push origin"**

---

## 📝 Примечания:

1. **Подсветка синтаксиса:**
   - Если файл большой (более 100KB), GitHub может не показывать подсветку синтаксиса
   - Это нормально! Файл всё равно будет работать
   - GitHub просто не показывает подсветку для больших файлов

2. **Размер файла:**
   - Если файл очень большой (более 1MB), GitHub может не дать загрузить через веб-интерфейс
   - В этом случае используйте Git через терминал или GitHub Desktop

3. **Проверка после загрузки:**
   - После загрузки проверьте, что файл доступен по адресу:
   - `https://github.com/gerol4lfun/teplitsa-calculator-main/blob/main/js/scripts.js`
   - Если файл открывается - всё в порядке!

---

## ✅ После загрузки:

1. Подождите 1-2 минуты
2. Проверьте, что файл доступен в репозитории
3. Обновите страницу GitHub Pages
4. Откройте консоль браузера (F12) и проверьте, что нет ошибок 404

**Удачи! 🚀**
