# 🚀 Импорт базы знаний - Простая инструкция

## Если у вас уже есть .env файл с ключами

Скрипт автоматически найдет `.env` файл в:
1. Корне проекта (`/delivery-calculator-test-main/.env`)
2. Папке telegram-bot (`/delivery-calculator-test-main/telegram-bot/.env`)

## Что нужно сделать:

### 1. Проверьте, что в .env есть ключи:

Откройте ваш `.env` файл (в корне проекта или в `telegram-bot/`) и убедитесь, что там есть:

```
SUPABASE_URL=https://dyoibmfdohpvjltfaygr.supabase.co
SUPABASE_SERVICE_ROLE_KEY=ваш_ключ_здесь
```

### 2. Запустите импорт:

```bash
cd /Users/pavelkulcinskij/Downloads/delivery-calculator-test-main

# Установите зависимости (если еще не установлены)
npm install

# Импортируйте данные
npm run kb:import
```

### 3. Проверьте результат:

После импорта вы увидите:
- ✅ Вставлено: X
- 🔄 Обновлено: Y
- ⏭️ Пропущено: Z

### 4. Обновите страницу калькулятора:

Нажмите F5 и откройте "База знаний" - должны увидеть 25 карточек!

---

## Если .env файла нет:

Создайте файл `.env` в корне проекта со следующим содержимым:

```
SUPABASE_URL=https://dyoibmfdohpvjltfaygr.supabase.co
SUPABASE_SERVICE_ROLE_KEY=ваш_service_role_key
```

Где взять `SUPABASE_SERVICE_ROLE_KEY`:
1. Supabase Dashboard → Settings → API
2. Скопируйте `service_role` key (секретный ключ)

---

**Готово! 🎉**
