# 📖 Инструкция: Загрузка калькулятора в GitHub

## 🎯 Проблема:
Сайт не работает, потому что в репозитории нет папок `css/` и `js/`, или `index.html` не обновлен.

## ✅ Решение: Загрузить правильную структуру

### ШАГ 1: Загрузить папку CSS

1. Откройте: `https://github.com/gerol4lfun/teplitsa-calculator-main`
2. Нажмите **"Add file"** → **"Upload files"**
3. Перетащите папку `css/` с файлом `styles.css` из вашего компьютера
4. Нажмите **"Commit changes"** (внизу справа)

### ШАГ 2: Загрузить папку JS

1. Нажмите **"Add file"** → **"Upload files"**
2. Перетащите папку `js/` с файлом `scripts.js` из вашего компьютера
3. Нажмите **"Commit changes"**

### ШАГ 3: Обновить index.html

1. Откройте файл `index.html` в репозитории
2. Нажмите на иконку **карандаша** (Edit this file)
3. Найдите строку 13 и убедитесь, что там:
   ```html
   <link rel="stylesheet" href="css/styles.css?v=49">
   ```
4. Найдите строку 312 и убедитесь, что там:
   ```html
   <script src="js/scripts.js?v=47"></script>
   ```
5. Если пути другие - исправьте их
6. Нажмите **"Commit changes"** (внизу справа)

### ШАГ 4: Проверить структуру

После загрузки в репозитории должна быть такая структура:

```
teplitsa-calculator-main/
├── index.html
├── css/
│   └── styles.css
└── js/
    └── scripts.js
```

### ШАГ 5: Подождать и проверить

1. Подождите 1-2 минуты для обновления GitHub Pages
2. Откройте: `https://gerol4lfun.github.io/teplitsa-calculator-main/`
3. Обновите страницу (Ctrl+F5 или Cmd+Shift+R)
4. Откройте консоль браузера (F12) и проверьте, что нет ошибок 404

**✅ Готово! Сайт должен работать!**

---

## 📝 Альтернативный способ (если не получается загрузить папки):

Если не получается загрузить папки целиком:

1. **Создать папку css:**
   - Нажмите "Add file" → "Create new file"
   - Введите: `css/styles.css` (важно: `css/` в начале!)
   - Скопируйте весь текст из локального файла `css/styles.css`
   - Нажмите "Commit new file"

2. **Создать папку js:**
   - Нажмите "Add file" → "Create new file"
   - Введите: `js/scripts.js` (важно: `js/` в начале!)
   - Скопируйте весь текст из локального файла `js/scripts.js`
   - **ВАЖНО:** Если файл большой (более 100KB), GitHub может не показывать подсветку синтаксиса - это нормально!
   - Нажмите "Commit new file"
   
   **Если GitHub не дает сохранить:**
   - Убедитесь, что в имени файла есть расширение `.js`
   - Попробуйте загрузить через "Upload files" вместо "Create new file"
   - Если файл очень большой, разбейте его на части или используйте Git через терминал

3. **Обновить index.html** (как в ШАГЕ 3 выше)

---

**Удачи! 🚀**
